mkdir -p grafana/provisioning
mkdir -p grafana/data
mkdir -p proprometheus/config
mkdir -p proprometheus/data

cd proprometheus/config
# prometheus-start.yml
# prometheus.yml
# prometheus-with-exporter.yml

# data sources